# Qt Creator user settings (Qt Creator 使用者設定檔)
*.pro.user
*.user

# CMake build directory (建置目錄與編譯檔案)
# ----------------------------------------
# 忽略 build/ 目錄下的所有內容
build/

# 但保留 build/Release/ (不忽略)
!build/Release/

# 如果 build/Release/ 下還有子目錄，且不想被忽略，也可以另外加上排除規則
# 例如，如果 Release/ 下有 my_release_binary.exe 想保留，而不想保留其他檔案：
# !build/Release/my_release_binary.exe

# CMake generated files (CMake 產生和產生的檔案)
CMakeCache.txt
!CMakeLists.txt
CMakeCache.txt.usr
CMakeFiles/
cmake_install.cmake
Makefile
CPackConfig.cmake
CTestTestfile.cmake

# Ninja build files (如果你用 CMake 搭配 Ninja 所生成的檔案)
.ninja_deps
.ninja_log

# Executables and libraries (可執行檔和靜態/動態函式庫)
*.exe
*.dll
*.lib
*.so
*.dylib

# Object files (編譯過程產生的目標檔)
*.obj
*.o
*.lo

# Debugging files (除錯用檔案)
*.pdb
*.ilk

# Qt specific files (Qt Designer 產生的 UI 標頭檔，通常由編譯過程自動產生)
ui_*.h
moc_*.cpp
qrc_*.cpp
*.qrc.cpp # 給較新 Qt 版本用的
*.json
icon/—Pngtree—wifi vector icon_4015278.png